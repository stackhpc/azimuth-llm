apiVersion: zenith.stackhpc.com/v1alpha1
kind: Reservation
metadata:
  name: azimuth-llm-ui
  labels:
    {{- include "azimuth-llm.labels" . | nindent 4 }}
spec:
  credentialSecretName: azimuth-llm-ui-zenith-credential