apiVersion: v1
kind: Service
metadata:
  name: gradio-app
  labels:
    {{- include "azimuth-llm.labels" . | nindent 4 }}
spec:
  ports:
  - name: ui
    port: 80
    protocol: TCP
    targetPort: ui
  selector:
    {{- include "azimuth-llm.ui-selectorLabels" . | nindent 4 }}